# Creative Director Agent Failure Log

Track rejected concepts, reasons, and correction patterns.

## Retry and Escalation

1. One revision cycle on concept rejection.
2. On second rejection, return `needs_review` with alternatives.
3. Escalate `blocked` only when constraints are mutually incompatible.
